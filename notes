In "data_base_needed_functionality" module:
- Add new method called update_user_password: it take new password and return true if it changed else false. (to change the password it should contain the main rules
  from Register_Sign Module)
